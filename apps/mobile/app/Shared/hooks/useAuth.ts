/**
 * useAuth Hook — Acceso simplificado al contexto de autenticación
 * 
 * Uso:
 * const { user, isAuthenticated, login, logout } = useAuth();
 */

export { useAuth } from '../../Providers/auth/AuthContext';
